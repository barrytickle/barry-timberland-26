{% if is_preview and preview_image %}
	<img src="{{ preview_image }}" alt="{{ block.title }} preview" loading="lazy" style="max-width:100%;">
{% else %}
{% set content = fields.content|default('') %}

<section class="mx-auto max-w-[1200px] px-4 py-10 sm:px-5 lg:py-14" data-component="body-text">
	<div class="rich-text-content article-prose mx-auto max-w-[72ch]">
		{{ content|strip_wp_classes }}
	</div>
</section>
{% endif %}
