{% set logo_text_highlight = options.logo_text_highlight|default('show') %}
{% set logo_text = options.logo_text|default('casy.') %}
{% set footer_tagline = options.footer_tagline|default('A stunning digital designer portfolio website template for you.') %}
{% set social_links = options.social_links|default([
	{ label: 'Instagram', url: '#' },
	{ label: 'Behance', url: '#' },
	{ label: 'Twitter', url: '#' },
	{ label: 'Dribbble', url: '#' }
]) %}
{% set default_column_1_links = [
	{ label: 'Home', url: site.url },
	{ label: 'About', url: site.url ~ '/about/' },
	{ label: 'Works', url: site.url ~ '/works/' },
	{ label: 'Insights', url: site.url ~ '/blog/' },
	{ label: 'Contact', url: site.url ~ '/contact/' }
] %}

{% set footer_title = options.footer_title|default('Page List') %}
{% set footer_links = options.footer_links %}

{% set copyright_text = options.copyright_text|default('©' ~ ("now"|date("Y")) ~ ' ' ~ site.name ~ '. All rights reserved.') %}


<footer class="bg-ink px-6 pb-10 pt-16 text-white lg:px-[72px]" data-component="footer">
	<div class="mx-auto max-w-[1200px]">
		<span class="text-3xl font-semibold tracking-tightest">
			<span class="text-ink-500">{{logo_text_highlight}}</span>{{logo_text}}
		</span>

		<p class="body-18 mt-5 max-w-md text-ink-300">
			{{footer_tagline}}
		</p>

		<div class="mt-10 grid grid-cols-2 gap-4 lg:grid-cols-4">
			{% for social in social_links %}<a class="rounded-full border border-ink-700 py-4 text-center body-16 font-medium transition hover:border-white" href="{{ social.url }}">{{ social.label }}</a>{% endfor %}
		</div>

		<div class="mt-14 grid gap-10 lg:grid-cols-[1.4fr_1fr]">
			<div>
				<p class="body-18 mb-6 font-semibold text-ink-400">{{footer_title}}</p>

				<div class="grid grid-cols-2 gap-x-8 gap-y-3 body-16 sm:grid-cols-4">
					{% for link in footer_links %}<a class="text-ink-200 hover:text-white" href="{{ link.url }}">{{ link.label }}</a>{% endfor %}
				</div>
			</div>

			<div>
				<p class="body-18 mb-6 font-semibold text-ink-400">Stay connected w/ me.</p>

				<div class="relative">
					<input class="w-full border-0 border-b border-ink-700 bg-transparent py-3 pr-10 body-16 text-white placeholder:text-ink-500 focus:border-white focus:outline-none focus:ring-0" placeholder="Enter your email" type="email">
					<button aria-label="Subscribe" class="absolute right-0 top-1/2 -translate-y-1/2 text-white">
						<svg class="h-5 w-5" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24">
							<path d="M7 17 17 7"></path>
							<path d="M7 7h10v10"></path>
						</svg>
					</button>
				</div>
			</div>
		</div>

		<p class="mt-14 border-t border-ink-800 pt-8 text-center body-14 text-ink-500">
			{{copyright_text}}
		</p>
	</div>
</footer>
